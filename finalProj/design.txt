Container (Multiple options):
max size 

    package:
        weight
        dimensions
        contents

        




Decorator Function
    Ship
        makes the package ( makePackage() )
        Outputs status of container
